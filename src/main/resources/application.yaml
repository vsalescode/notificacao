spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: email@example.com
    password: senha-google-apps
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

thymeleaf:
  enabled: true

envio:
  email:
    remetente: email@example.com
    nomeRemetente: Sistema de Notificações

server:
  port: 8082
